#!/usr/bin/env bash
# Blocks commits that would add secrets or private content.
# Enable once per clone:  git config core.hooksPath .githooks
exec bash "$(git rev-parse --show-toplevel)/scripts/scan.sh"
